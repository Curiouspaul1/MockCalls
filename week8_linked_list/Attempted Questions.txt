Question -> You are given the heads of two sorted linked lists list1 and list2.
Merge the two lists into one sorted list. The list should be made by splicing together the nodes of the first two lists.

Return the head of the merged linked list.

Example 1:
Input: list1 = [1,2,4], list2 = [1,3,4]
Output: [1,1,2,3,4,4]

Example 2:
Input: list1 = [], list2 = []
Output: []

Solution ->
function mergeTwoLists(list1, list2) {
  let mergedList = list1; // or [];
  // 1 -> 2 -> 2 -> 1 -> 4 -> 3 -> 4

  while(list2 !== null && list2.next !== null) {
    let tempNext = list2.next;
    mergedList.next = list2;
    list2.next = list2.next.next;
  }
 

  while(mergedList !== null && mergedList.next !== null) {
    if(mergedList.val < mergedList.next) {
    
    }
  }
}
